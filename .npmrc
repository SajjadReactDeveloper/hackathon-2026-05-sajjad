# Hoist Prisma packages to root node_modules so @prisma/client generated types
# are accessible across all workspaces (required for pnpm + Prisma in monorepos).
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=@prisma*
