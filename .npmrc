# Hoist all packages to root node_modules (npm-style flat layout).
# Required for pnpm + Prisma in monorepos so @prisma/engines native binaries
# are accessible at a real path and prisma generate doesn't hang on Render.
shamefully-hoist=true
