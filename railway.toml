[build]
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @repo/types build && pnpm --filter @repo/ai build && pnpm --filter @repo/db build && pnpm --filter @repo/api build"

[deploy]
startCommand = "node apps/api/dist/main"
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 3
